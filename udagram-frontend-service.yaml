apiVersion: v1
kind: Service
metadata:
  name: udagram-frontend
  labels:
    run: udagram-frontend
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8100
  selector:
    run: udagram-frontend